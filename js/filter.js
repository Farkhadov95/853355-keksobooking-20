'use strict';
(function () {

  // window.filterHousingTypes = function (pointsArray, nameType) {
  //   var filteredArray = pointsArray.filter(function (it) {
  //     return it.offer.type === nameType;
  //   });
  //   window.renderPins(filteredArray);
  // };

  // Как привязать select к фильтру?
})();
